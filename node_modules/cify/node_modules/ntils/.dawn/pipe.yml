build:
  - name: shell
    script:
      - ./node_modules/.bin/rollup -i src/index.js -o lib/index.js -f cjs -n ${project.name}
      - ./node_modules/.bin/rollup -i src/index.js -o dist/${project.name}.js -f umd -n ${project.name}

dev:
  - name: shell
    script:
      - dn build